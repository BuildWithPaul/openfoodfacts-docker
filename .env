# Open Food Facts local API configuration
OFF_PORT=5003

# PostgreSQL
POSTGRES_USER=off
POSTGRES_PASSWORD=off
POSTGRES_DB=openfoodfacts

# Data import: which dataset to load
# Options: sample (~200 products), french (~500K), full (~3M)
IMPORT_DATASET=french